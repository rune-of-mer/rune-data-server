name: Publish
description: Publish the Docker package
inputs:
  major:
    description: Major version number
    required: true
  minor:
    description: Minor version number
    required: true
  patch:
    description: Patch version number
    required: true
  sha:
    description: Git SHA
    required: true

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v6

    - uses: docker/setup-buildx-action@v3

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ github.token }}

    - name: Build and push
      uses: docker/build-push-action@v6
      with:
        push: true
        platforms: linux/amd64,linux/arm64
        file: ./docker/Dockerfile
        tags: |
          ghcr.io/rune-of-mer/rune-data-server:latest
          ghcr.io/rune-of-mer/rune-data-server:${{ inputs.sha }}
          ghcr.io/rune-of-mer/rune-data-server:v${{ inputs.major }}
          ghcr.io/rune-of-mer/rune-data-server:v${{ inputs.major }}.${{ inputs.minor }}
          ghcr.io/rune-of-mer/rune-data-server:v${{ inputs.major }}.${{ inputs.minor }}.${{ inputs.patch }}